# 値上がりウォッチ セットアップ手順

プログラミングの知識がなくても大丈夫です。上から順番にコピー&貼り付けしていけば完成します。
分からないところがあったら、画面のスクリーンショットを見せてください。一緒に確認します。

---

## 全体の流れ(4ステップ)

1. GitHubに「リポジトリ」という置き場所を作り、このファイル一式をアップロードする
2. APIキーと通知先を「秘密の設定」として登録する(誰にも見えない場所に保存されます)
3. サイトを表示する機能(GitHub Pages)をONにする
4. スマホに通知アプリ(ntfy)を入れる(エラー時にお知らせが届きます)

---

## ステップ1: リポジトリを作ってファイルをアップロード

1. ブラウザで https://github.com/new を開く(change0616-debug でログインした状態で)
2. 「Repository name」に `pokemon-price-tracker` と入力
3. 「Public」を選ぶ(Privateだと後述のGitHub Pagesが使えない場合があります)
4. 「Create repository」ボタンを押す
5. 作成された画面で「uploading an existing file」というリンクをクリック
6. お渡しした一式のファイル・フォルダ(このメッセージに添付されているzipファイルの中身)を、**フォルダ構造を保ったまま**すべてドラッグ&ドロップ
   - `.github/workflows/daily-fetch.yml` のように、フォルダの中にファイルが入っている構造も、そのままドラッグすれば維持されます
7. 一番下の「Commit changes」ボタンを押してアップロード完了

---

## ステップ2: 有料プラン・APIキー・通知先を登録する

1. https://www.pokemonpricetracker.com/pricing を開く
2. 「API」プラン(月$9.99)を選んで申し込む
3. 登録後、ダッシュボードで発行される新しいAPIキーをコピーする
   (無料プランの時のキーとは別のものが発行されることがあります)
4. リポジトリの「Settings」タブを開く
5. 左メニューの「Secrets and variables」→「Actions」をクリック
6. 「New repository secret」ボタンを押し、以下を登録
   - Name: `POKEMON_PRICE_API_KEY`
   - Secret: (コピーしたAPIキーを貼り付け)
7. もう一度「New repository secret」を押し、通知用のIDも登録する
   - Name: `NTFY_TOPIC`
   - Secret: `pokemon-uo-` に続けて、自分だけの適当な英数字を足したもの
     (例: `pokemon-uo-8x2k91`。他人に推測されない文字列にしてください。これが分かると誰でもあなたに通知を送れてしまいます)
   - ※ この文字列は次の「ステップ4」で使うので、メモしておいてください

---

## ステップ3: サイトを表示できるようにする(GitHub Pages)

1. 同じ「Settings」タブの中の「Pages」をクリック
2. 「Branch」を `main` に、フォルダは `/ (root)` のまま選んで「Save」
3. 少し待つと、ページ上部に「Your site is live at https://change0616-debug.github.io/pokemon-price-tracker/」のようなURLが表示されます
   - これが完成したサイトのURLです。ブックマークしておくと便利です

---

## ステップ4: スマホに通知アプリを入れる(エラー通知用)

1. iPhoneの App Store で「ntfy」を検索してインストール(無料・登録不要)
2. アプリを開き、「+」ボタンで新しいトピックを購読する
3. ステップ2で決めた `NTFY_TOPIC` の文字列(例: `pokemon-uo-8x2k91`)を入力して購読
4. これで、GitHub Actionsが失敗した時にこのアプリへ通知が届きます

---

## ステップ5: 動作確認(最初の1回は手動で実行してみる)

1. リポジトリの「Actions」タブを開く
2. 左側の「Daily price fetch」をクリック
3. 右側に出てくる「Run workflow」ボタン→もう一度「Run workflow」を押す
4. 10分ほど待って、緑のチェックマークが付けば成功です
5. サイトのURLを開くと、ランキングが表示されるはずです

**重要: 初回は必ず結果を確認してください**

PSA10(鑑定品)のデータの取り出し方は、実際のAPIでまだ一度も検証できていません。もし初回実行後にサイトに「PSA10データが取得できていません」という警告が出た場合は、リポジトリの `data/debug/latest_sample.json` というファイルの中身をコピーして見せてください。実際のデータ形式に合わせてすぐに修正します。

---

## その後について

- 毎日、日本時間の朝6時ごろに自動でデータが更新されます(何もしなくてOKです)
- PSA10(鑑定満点)の価格を基準に、「3ヶ月前と比べて2倍(+100%)以上」かつ「上昇額+5000円以上」のカードだけが表示されます
- サイト上の「海外のみ」「日本のみ」ボタンで絞り込みができます
- 各カードに、直近の価格推移の簡易グラフと、「継続上昇」「直近急騰」のラベルが付きます
- サイト上部のプルダウンから、過去の日付のランキングも振り返って見られます(直近120日分を保存)
- 実行が失敗した場合は、スマホのntfyアプリに通知が届きます

---

## もし困ったら

- Actionsタブで赤い✕マークが出た場合は、そこをクリックするとエラーの内容が見られます。スクリーンショットを見せていただければ一緒に直します(ntfyにも通知が届くはずです)
- 1日のクレジット上限(20,000)に近づいた場合、その日は途中までの結果になり、サイトに「この日はAPIの上限により途中まで」と表示されます。翌日には自動的に続きが処理されるので、特に対応は不要です。
